# Google Drive OAuth (Web client ID from Google Cloud Console).
# Authorized JavaScript origins must include your app URL, e.g.:
#   https://localhost:8080
#   http://localhost:8080
# Without this, Settings → Sync falls back to .slfv file mode.
VITE_SLFV_GDRIVE_CLIENT_ID=
