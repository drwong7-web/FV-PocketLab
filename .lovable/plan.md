Supprimer les textes d'exemple (placeholders) des deux champs du formulaire de création de profil dans `src/pages/Auth.tsx` :
- Champ nom : retirer `placeholder="Coach Smith"`
- Champ équipe : retirer `placeholder="FFA — Pôle Sprint"`

Aucune autre modification n'est requise ; les labels et la validation restent inchangés.